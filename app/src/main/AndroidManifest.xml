<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.abc_connected">


    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission
        android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ABC_connected">
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".frontend.user_ui.calendario.DailyCalendarActivity"
            android:exported="true" />
        <activity android:name=".frontend.user_ui.calendario.EventEditActivity" />
        <activity android:name=".frontend.user_ui.calendario.WeekViewActivity" />
        <activity android:name=".frontend.user_ui.calendario.MainActivityCalendar" />


    </application>

</manifest>